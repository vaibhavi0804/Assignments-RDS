# 12. Create a vector from (8.) that repeats the third element of (8.) three times,
# the sixth element four times, and the last element once.

vec12 <- c(rep(vec8[3], 3), rep(vec8[6], 4), vec8[length(vec8)])